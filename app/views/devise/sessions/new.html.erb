<div class="container-fluid">
  <h1 class="text-center fs-4 mb-4"><%= t 'devise.sessions.new.sign_in' %></h1>

  <%= form_with model: @user, url: session_path(resource_name), data: { turbo: false }, local: true, class: "row g-3 align-items-center" do |f| %>

    <div class="mb-3 row justify-content-center">
      <div class="field">
        <%= f.label :email, class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>
    </div>

    <div class="mb-3 row justify-content-center">
      <div class="field">
        <%= f.label :password, class: "form-label" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
      </div>
    </div>

    <div class="row justify-content-center mb-2">
      <% if devise_mapping.rememberable? %>
        <div class="field form-check ps-5">
          <%= f.check_box :remember_me, class: "form-check-input" %>
          <%= f.label :remember_me, class: "form-check-label" %>
        </div>
      <% end %>
    </div>

    <div class="actions text-center my-3">
      <%= f.submit t('devise.sessions.new.sign_in'), class: "btn btn-outline-success text-wrap" %>
    </div>
  <% end %>

  <div class="text-center mb-3">
    <%= link_to t('devise.shared.links.back'), root_path, class: "btn btn-outline-primary text-wrap" %>
  </div>

  <div class="text-center my-3">
    <%= render "devise/shared/links" %>
  </div>
</div>
