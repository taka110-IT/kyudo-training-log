<div class="container-fluid">
  <h1 class="text-center fs-4 my-3"><%= t 'views.new.title' %></h1>

  <%= render "form", practice: @practice %>

  <div class="back_link text-end mt-4">
    <%= link_to t('views.common.back'), root_path, data: { turbo_frame: "_top" }, class: "text-secondary" %>
  </div>
</div>
