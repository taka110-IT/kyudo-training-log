<div class="container-fluid col-8">
  <h1 class="fs-2 text-center my-3"><%= t 'views.edit.title' %></h1>

  <%= render "form", practice: @practice %>

  <div class="text-center mb-3">
    <%= link_to t('views.common.destroy'), @practice, data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？", turbo_frame: "_top" }, class: "btn btn-outline-danger col-3 text-wrap" %>
  </div>

  <div class="text-center">
    <%= link_to t('views.common.back'), practices_path, data: { turbo_frame: "_top" }, class: "btn btn-outline-primary col-3 text-wrap" %>
  </div>
</div>
