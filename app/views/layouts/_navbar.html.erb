<nav class="navbar">
  <div class="container-fluid pb-3 border-bottom">
    <div>
      <%= link_to image_tag('title.png', alt: t('layouts.application.title'), class: "img-fluid"), root_path, data: { turbo_frame: "_top" } %>
    </div>
    <div class="ms-auto btn-group">
      <a class="btn btn-outline-success btn-sm dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        メニュー
      </a>
    <% if user_signed_in? %>
        <ul class="dropdown-menu dropdown-menu-end">
          <li><%= link_to t('layouts.application.edit_profile'), edit_user_registration_path, class: 'dropdown-item' %></li>
          <li><%= link_to t('layouts.application.logout'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'dropdown-item' %></li>
        </ul>
      <% else %>
        <ul class="dropdown-menu dropdown-menu-end">
          <li><%= link_to t('devise.shared.links.sign_up'), new_user_registration_path, class: 'dropdown-item' %></li>
          <li><%= link_to t('devise.shared.links.sign_in'), new_user_session_path, class: 'dropdown-item btn btn-outline-success' %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
