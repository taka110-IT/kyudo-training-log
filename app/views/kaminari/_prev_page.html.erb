<li class="page-item">
  <% if current_page.first? %>
    <span class="page-link disabled" ><%= raw(t 'views.pagination.previous') %></span>
  <% else %>
    <%= link_to raw(t 'views.pagination.previous'), url, rel: 'prev', remote: remote, class: 'page-link' %>
  <% end %>
</li>
