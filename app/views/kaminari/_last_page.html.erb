<li class="page-item">
  <% if current_page.last? %>
    <span class="page-link disabled" ><%= raw(t 'views.pagination.last') %></span>
  <% else %>
    <%= link_to raw(t 'views.pagination.last'), url, remote: remote, class: 'page-link' %>
  <% end %>
</li>
