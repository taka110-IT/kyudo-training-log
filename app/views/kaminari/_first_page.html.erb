<li class="page-item">
  <% if current_page.first? %>
    <span class="page-link disabled" ><%= raw(t 'views.pagination.first') %></span>
  <% else %>
    <%= link_to raw(t 'views.pagination.first'), url, remote: remote, class: 'page-link' %>
  <% end %>
</li>
